#!/bin/sh

# Converts text

from="${1:-WINDOWS-1251}"
to="${2:-UTF-8}"
res="${3:-converted}"

rm -rf $res;
mkdir -p $res;
find . -type f -exec bash -c 'iconv -f '$from' -t '$to' "{}" > "'$res'/{}"' \;
